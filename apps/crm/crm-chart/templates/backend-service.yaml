apiVersion: v1
kind: Service
metadata:
  name: crm-backend
  labels:
    {{- include "crm.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: crm-backend
  ports:
    - port: {{ .Values.backend.port }}
      targetPort: {{ .Values.backend.port }}
