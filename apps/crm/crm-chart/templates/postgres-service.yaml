apiVersion: v1
kind: Service
metadata:
  name: crm-postgres
  labels:
    {{- include "crm.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: crm-postgres
  ports:
    - port: 5432
      targetPort: 5432
