{{- if .Values.aiAdvisor.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: crm-ai-advisor-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  crm-ai-advisor-dashboard.json: |
{{ .Files.Get "dashboards/crm-ai-advisor-dashboard.json" | indent 4 }}
{{- end }}
