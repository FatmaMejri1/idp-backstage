apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: crm-postgres-pvc
  labels:
    {{- include "crm.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.postgres.storage }}
