app:
  title: Scaffolded Backstage App
  # Use localhost so Windows browsers reach WSL via localhost forwarding.
  # Bind 0.0.0.0 so the dev server is reachable from Windows and the WSL IP.
  baseUrl: http://localhost:3000
  listen:
    host: 0.0.0.0
    port: 3000

  # Enable all packages by default, this will discover packages from packages/app/package.json
  packages: all

  extensions:
    # Default landing page configuration.
    # The catalog is mounted at /catalog by default but is overridden here to
    # serve as the root page. To switch to the Home page, comment out the
    # catalog path override and uncomment the home path override.
    - page:catalog:
        config:
          path: /

    # CRM alerts are PrometheusRules, not Grafana-managed unified alerts.
    - entity-card:grafana/alerts: false

    # - page:home:
    #     config:
    #       path: /

    # Visit tracking records which pages users navigate to and is disabled by default.
    # Enable it to populate the Most Visited and Recently Visited widgets on the Home page.
    # Visit data is stored via UserSettings storage if available, otherwise browser localStorage.
    # - api:home/visits: true
    # - app-root-element:home/visit-listener: true

    - home-page-widget:home/toolkit:
        config:
          tools:
            - url: https://backstage.io/docs
              label: Docs
              icon: docs
            - url: https://github.com/backstage/backstage
              label: GitHub
              icon: github
            - url: https://github.com/backstage/backstage/blob/master/CONTRIBUTING.md
              label: Contributing
              icon: docs
            - url: https://backstage.io/plugins
              label: Plugins Directory
              icon: kind:component
            - url: https://github.com/backstage/backstage/issues/new/choose
              label: Submit New Issue
              icon: github

    - home-page-widget:home/world-clock:
        config:
          clockConfigs:
            - label: NYC
              timeZone: America/New_York
            - label: UTC
              timeZone: UTC
            - label: STO
              timeZone: Europe/Stockholm
            - label: TYO
              timeZone: Asia/Tokyo

    - page:home:
        config:
          defaultConfig:
            - component: HomePageSearchBar
              column: 0
              row: 0
              width: 12
              height: 2
              deletable: false

            - component: GettingStarted
              column: 0
              row: 1
              width: 4
              height: 11

            - component: HomePageStarredEntities
              column: 4
              row: 2
              width: 4
              height: 4

            - component: HomePageRandomJoke
              column: 8
              row: 2
              width: 4
              height: 4

            - component: HomePageToolkit
              column: 4
              row: 6
              width: 4
              height: 3

            - component: HomePageWorldClock
              column: 8
              row: 6
              width: 4
              height: 3

            - component: HomePageMostVisited
              column: 4
              row: 9
              width: 4
              height: 4

            - component: HomePageRecentlyVisited
              column: 8
              row: 9
              width: 4
              height: 4

# Required env vars for local demo:
#   GITHUB_TOKEN, K8S_CLUSTER_URL, K8S_SA_TOKEN, PAGERDUTY_TOKEN
organization:
  name: My Company

backend:
  # Used for enabling authentication, secret is shared by all backend plugins
  # See https://backstage.io/docs/auth/service-to-service-auth for
  # information on the format
  # auth:
  #   keys:
  #     - secret: ${BACKEND_SECRET}

  # Browser API calls use this URL. localhost works from Windows via WSL port
  # forwarding; the process still binds 0.0.0.0 so Kind/Docker can reach :7008.
  baseUrl: http://localhost:7008

  listen:
    port: 7008

    # Kind Alertmanager reaches this webhook via host.docker.internal:7008
    host: 0.0.0.0

    # Uncomment the following host directive to bind to specific interfaces
    # host: 127.0.0.1

  csp:
    connect-src: ["'self'", 'http:', 'https:']
    # Content-Security-Policy directives follow the Helmet format: https://helmetjs.github.io/#reference
    # Default Helmet Content-Security-Policy values can be removed by setting the key to false

  cors:
    origin:
      - http://localhost:3000
      - http://127.0.0.1:3000
    methods: [GET, HEAD, PATCH, POST, PUT, DELETE]
    credentials: true

  # This is for local development only, it is not recommended to use this in production
  # The production database configuration is stored in app-config.production.yaml
  # Persist locally so auth signing keys survive yarn restart (in-memory DB caused 401s)
  database:
    client: better-sqlite3
    connection:
      directory: ./sqlite

  # see https://backstage.io/docs/ai/mcp-actions#actions-configuration for more details
  actions:
    pluginSources:
      - auth
      - catalog
      - scaffolder

  # workingDirectory: /tmp # Use this to configure a working directory for the scaffolder, defaults to the OS temp-dir

integrations:
  github:
    - host: github.com
      # Token is set in app-config.local.yaml via GITHUB_TOKEN env var.
      # This entry is required for catalog:register to resolve GitHub repoContentsUrl
      # after scaffolding. No schedule = no auto-discovery of external repos.
      token: ${GITHUB_TOKEN:}

      ### Example for how to add your GitHub Enterprise instance using the API:
      # - host: ghe.example.net
      #   apiBaseUrl: https://ghe.example.net/api/v3
      #   token: ${GHE_TOKEN}

proxy:
  endpoints:
    # Used by @backstage/plugin-pagerduty (calls /api/proxy/pagerduty/*)
    '/pagerduty':
      target: 'https://api.pagerduty.com'
      headers:
        Authorization: 'Token token=${PAGERDUTY_TOKEN:}'
        Accept: 'application/vnd.pagerduty+json;version=2'

    '/github/api':
      target: 'https://api.github.com'
      changeOrigin: true
      headers:
        Authorization: 'token ${GITHUB_TOKEN:}'
        Accept: 'application/vnd.github+json'
        X-GitHub-Api-Version: '2022-11-28'

    # Grafana plugin + Alerts tab (kube-prometheus-stack default user/password
    # on this cluster is admin / admin123). Override with GRAFANA_AUTH_HEADER
    # (e.g. "Bearer glsa_…") if you rotate Grafana credentials.
    '/grafana/api':
      target: 'http://localhost:3300'
      changeOrigin: true
      headers:
        Authorization: '${GRAFANA_AUTH_HEADER:Basic YWRtaW46YWRtaW4xMjM=}'

# Reference documentation http://backstage.io/docs/features/techdocs/configuration
# Note: After experimenting with basic setup, use CI/CD to generate docs
# and an external cloud storage when deploying TechDocs for production use-case.
# https://backstage.io/docs/features/techdocs/how-to-guides#how-to-migrate-from-techdocs-basic-to-recommended-deployment-approach
techdocs:
  builder: 'local'
  generator:
    runIn: 'local'
  publisher:
    type: 'local'

auth:
  # see https://backstage.io/docs/auth/ to learn about auth providers
  providers:
    # See https://backstage.io/docs/auth/guest/provider
    guest:
      userEntityRef: user:default/guest
      ownershipEntityRefs:
        - group:default/guests
      dangerouslyAllowSignInWithoutUserInCatalog: true

  # see https://backstage.io/docs/ai/mcp-actions#client-id-metadata-documents
  # to learn more about client id metadata documents
  clientIdMetadataDocuments:
    enabled: false

    # Optional: restrict which client_id URLs are allowed (defaults to ['*'])
    # allowedClientIdPatterns:
    #   - 'https://example.com/*'
    #   - 'https://*.trusted-domain.com/*'

    # Optional: restrict which redirect URIs are allowed (defaults to ['*'])
    # allowedRedirectUriPatterns:
    #   - 'http://localhost:*'
    #   - 'https://*.example.com/*'

scaffolder:
  # see https://backstage.io/docs/features/software-templates/configuration for software template options

catalog:
  import:
    entityFilename: catalog-info.yaml
    pullRequestBranchName: backstage-integration

  rules:
    - allow: [Component, System, API, Resource, Location, Template, User, Group]

  locations:
    # CRM Component
    - type: file
      target: ../../../apps/crm/catalog-info.yaml

    # File locations are resolved from packages/backend, not the repo root.
    - type: file
      target: ./examples/org.yaml

    - type: file
      target: ./examples/template/template.yaml

    - type: file
      target: ./examples/golden-path-application/template.yaml
    - type: file
      target: ./examples/golden-path-service/template.yaml
    - type: file
      target: ./examples/golden-path-ai-service/template.yaml


kubernetes:
  serviceLocatorMethod:
    type: 'multiTenant'
  clusterLocatorMethods:
    - type: config
      clusters:
        - url: ${K8S_CLUSTER_URL:https://127.0.0.1:35507}
          name: kind-idp-backstage
          title: Kind Cluster
          authProvider: serviceAccount
          skipTLSVerify: true
          skipMetricsLookup: true
          serviceAccountToken: ${K8S_SA_TOKEN:}

grafana:
  # kind extraPortMappings: container 30300 -> host 3300
  domain: http://localhost:3300
  unifiedAlerting: true

  # Alertmanager datasource UID (prometheus type) in Grafana
  alertmanagerDataSource: alertmanager

  # kube-prometheus-stack Prometheus datasource UID used by the Alerts card
  prometheusDataSource: prometheus

# Alertmanager posts firing/resolved alerts to POST /api/alertmanager/webhook
alertmanager:
  webhookToken: ${ALERTMANAGER_WEBHOOK_TOKEN:}

  # Guest login should see firing CRM alerts in the notification bell.
  recipientEntityRef: user:default/guest

pagerDuty:
  eventsBaseUrl: 'https://events.pagerduty.com/v2'
  apiToken: ${PAGERDUTY_TOKEN:}

# see https://backstage.io/docs/permissions/getting-started for more on the permission framework
permission:
  # setting this to `false` will disable permissions
  enabled: false

# see https://backstage.io/docs/ai/mcp-actions for more details
mcpActions:
  name: 'My Company Backstage'
  description: 'Tools for managing your software catalog, creating new services from templates, and exploring your developer portal'