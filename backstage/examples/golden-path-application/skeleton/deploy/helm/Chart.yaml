apiVersion: v2
name: ${{ values.name }}
description: Helm chart for the ${{ values.name }} Golden Path Application (Frontend + Backend + AI Service + PostgreSQL)
type: application
version: 0.1.0
appVersion: "1.0.0"
